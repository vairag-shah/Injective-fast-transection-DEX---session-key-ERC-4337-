if (typeof React === 'undefined') await import('https://unpkg.com/react@16/umd/react.production.min.js')

/* props {
 *   assets: {
 *     lolSrc: 'lol.jpg',
 *   },
 *   styles: {
 *     img: { height: '100%', width: '100%', maxWidth: '268px' },
 *     div: { backgroundColor: 'pink' },
 *   },
 *   clickMessage: 'hey',
 * }
 */
const HeySayer = (props, children = []) => {
  const { styles, assets, clickMessage } = props
  const [clicked, setClicked] = useState(false)
  useEffect(() => {
    console.log('I\'m an effect of HeySayer!')
    return () => {
      console.log('I clean up the effect.')
    }
  })
  return Div({
    id: 'say-hey-once',
    style: styles.div,
  }, [
    Img({
      src: assets.lolSrc,
    }),
    Button(({
      onClick: () => {
        console.log(clickMessage)
        setClicked(true),
      }
    }), ['say hey once']),
  ])
}
